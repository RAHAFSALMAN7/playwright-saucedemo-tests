{
  "name": "saucedemo-login-test",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.0",
    "dotenv": "^16.0.0",
    "typescript": "^5.0.0"
  }
}
